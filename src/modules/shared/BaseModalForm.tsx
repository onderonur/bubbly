import BaseForm from '@src/modules/shared/BaseForm';
import styled from 'styled-components';

const BaseModalForm = styled(BaseForm)`
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
`;

export default BaseModalForm;
